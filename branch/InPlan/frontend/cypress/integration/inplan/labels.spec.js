/*
 * InPlan end-2-end tests
 */

/*
describe("labels", () => {
  beforeEach(() => {
    cy.resetdb();
    cy.login();
  });

  it("label_page", () => {
    cy.get("[data-test=label_page]").click();
    cy.get("[data-test=batch_name]").should("have.length", 1);
  });
});
*/
